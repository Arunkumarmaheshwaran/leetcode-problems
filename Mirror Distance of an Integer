class Solution {
    public int mirrorDistance(int n) {
        int rev_num = 0;
        int temp = n;
        while(temp != 0){
            int rem = temp % 10;
            rev_num = rev_num * 10 + rem;
            temp /=10;
        }
        return Math.abs(n-rev_num);
    }
}
