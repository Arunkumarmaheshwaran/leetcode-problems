class Solution {
    public int subtractProductAndSum(int n) {
        int mul=1,add=0;
        while(n>0){
            int r=n%10;
            add+=r;
            mul*=r;
            n/=10;
        }
        return mul-add;
    }
}
